# Legacy asset tools (PySide6) — **deprecated**

This directory is **not** the supported editor. It predates the Fluent-based app and is **partially broken** (e.g. `PowerUI` import mismatch, undefined `BUILDING_TYPES`, stub/invalid `rail_pages` / `road_pages`).

**Use instead:** [`../../src/utils/asset_tools/README.md`](../../src/utils/asset_tools/README.md)

**Why keep this folder (for now):** historical reference and occasional copy of regex/helpers. **Do not** add new features here.

**PyQt5 ports** of legacy helpers (`ClickableComboBox`, etc.): `../../src/utils/asset_tools/src/components/editor_widgets.py`

**Audit:** `prompts/designer_questions/tools_ui/spec/07_asset_editor_dual_chain_audit_v1.md`
