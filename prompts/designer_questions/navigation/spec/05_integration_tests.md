# Navigation — integration tests `05`

1. **Chunk seam:** same seed, path crosses chunk boundary — identical polyline after load order permutations 📎.
2. **LOD handoff:** forced downgrade/upgrade — no oscillation beyond threshold 📎.
3. **Rail block:** simple two-train setup — deadlock prevention or expected wait (document expected behavior).

Place tests under `tests/` or feature-gated harness per project convention.
