# Tools UI — integration tests `05`

1. **Toggle:** devtools feature on — open/closeDiagnostics panel — no panic; egui ctx present.
2. **Input:** simulate key chord — egui consumes vs game (expected behavior documented).
3. **Headless:** tools plugin registered but **windowless** run skips draw paths without error 📎.
