# Designer Q — navigation & pathfinding

**Paired matrix:** `prompts/matrix/repo/` (layer rules) + future dedicated nav matrix if split from repo.

> **Prompt use:** Layer rules from repo matrix first · cite path to pathfinding stubs in `src/` · `ASK:` for MP authority model if unset.

| File / folder | Role |
|:---|:---|
| **`spec/README.md`** | **Start here** — structured index `00`–`05` |
| `spec/00_scope.md` … `spec/05_*.md` | Scope, graphs, LOD, MP, perf, tests |
| `pathfinding_hierarchical_v1.md` | Hybrid LOD pathing, rail vs road, client propose / server validate |
| **`implementation_questions_v1.md`** | **Engineering checklist** |
